{
  "// note": "Type-checking foundation (docs/IMPROVEMENT-PLAN.md Phase 1). checkJs is off for now; flip to true file-by-file with // @ts-check or globally once the Config typedef in types.d.ts is fleshed out. `npm run typecheck` validates the declared types and any future .ts files.",
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "allowJs": true,
    "checkJs": false,
    "noEmit": true,
    "strict": true,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "jsx": "react-jsx",
    "types": []
  },
  "include": ["types.d.ts", "src/**/*", "server/**/*", "vite.config.js"],
  "exclude": ["node_modules", "dist", "coverage"]
}
